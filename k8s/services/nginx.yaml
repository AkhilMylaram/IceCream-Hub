apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: icecream-hub
spec:
  type: NodePort # Typical for external ingress in simple setups
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30001
      protocol: TCP
  selector:
    app: nginx

