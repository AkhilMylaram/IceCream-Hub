# Official Apache Kafka image
FROM apache/kafka:3.7.0

# You can add custom configuration or scripts here in the future
# e.g., COPY server.properties /etc/kafka/server.properties

# Kafka usually runs as 'kafka' user in this image, 
# but settings are handled via environment variables in docker-compose for flexibility.
