apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  namespace: icecream-hub
spec:
  ports:
    - port: 8090
      targetPort: 8080
      nodePort: 30002
      protocol: TCP
  selector:
    app: kafka-ui
  type: NodePort

